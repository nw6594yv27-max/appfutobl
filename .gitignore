venv/
__pycache__/
*.pyc
.DS_Store

# Videos de prueba (pesados / datos personales). El modelo de MediaPipe
# (modelos/) SI se versiona, para que este disponible en Render sin depender
# de una descarga durante el build.
videos/
uploads/

# Historial de analisis (datos personales de jugadores, no se versiona)
historial.db
